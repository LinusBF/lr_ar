name: lr_ar
description: An Augmented Reality app made for long range use cases where plane detection isn't needed
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  camera: ^0.5.8+7
  geolocator: ^6.1.1
  flutter_compass: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

